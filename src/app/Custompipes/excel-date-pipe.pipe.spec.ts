import { ExcelDatePipePipe } from './excel-date-pipe.pipe';

describe('ExcelDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExcelDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
